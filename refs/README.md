# 参考画面

クラッシュロワイヤルのスクリーンショットをここに置く。
`node scripts/extract-ref-colors.mjs` で色・寸法を数値として抜き出し、
`tokens.css` に反映する。

目で見て似せると質感が決定的に劣るので、必ず数値で持ってくること。

| ファイル名 | 画面 |
|---|---|
| `battle.png` | バトル中（手札・next・エリクサーバーが見える。最重要） |
| `home.png` | ホーム |
| `deck.png` | デッキ編成 |
| `result.png` | 対戦結果 |
